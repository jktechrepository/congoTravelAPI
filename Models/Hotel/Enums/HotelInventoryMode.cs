namespace CongoTravel.Models.Hotel.Enums
{
    /// <summary>Mode d'inventaire hôtel — exclusif (pas d'hybride).</summary>
    public enum HotelInventoryMode
    {
        ClassQuota,
        GlobalQuota
    }
}
